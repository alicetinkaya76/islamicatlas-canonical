[pytest]
testpaths = tests/integration
markers =
    slow_fullstore: validates every canonical record against its schema (~13 s on the 46,702-record store); skip for the fast inner loop with -m "not slow_fullstore"
