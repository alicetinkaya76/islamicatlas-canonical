# Hafta 10 — Stage 10: Typesense canlı yolu (emit + upsert)

**Date:** 2026-07-10 · **Entry:** Stage 9 (`9e93819`) üstüne.
Faz 0.5'in "canlı Typesense" kalemi (PHASE0_CLOSEOUT §4) — v2 servis
yığınının ilk parçası. Kod TAM; ilk canlı koşu HOSTING KARARINA bağlı
(env-korumalı — URL/API-key yokken bilinçli net hata).

## Teslim edilenler

- **`search/typesense_schema_emit.py`**: annotated koleksiyon şemasından
  ($id/comment'li proje-kaynağı) saf Typesense create-collection gövdesi
  (34 alan, `iac_entities`; dok-anahtarı sızıntısı testli-0). Şema
  dosyasının H2'den beri verdiği "generated by emit" sözü artık gerçek.
- **`pipelines/search/upsert.py`**: koleksiyon-oluştur (+ `--recreate`) →
  NDJSON'u 1.000'lik parçalarla `import?action=upsert` → satır-satır sonuç
  SAYILIR, başarısızlar raporlanır (sessiz kayıp yok). NDJSON verilmezse
  projector'dan taze üretir. Projector docstring'inin "Used by upsert.py"
  sözü de artık gerçek.
- **Makefile**: `emit-collection` + `upsert-live` hedefleri.
- **Sözleşme testleri (+2 → 158)**: (1) emit gövdesi temiz + `_score`
  default-sort alanı mevcut-ve-float (canlı create'in patlama noktası);
  (2) örneklemli GERÇEK projeksiyonlar yalnız ilan edilmiş alanları taşıyor
  (canlı import'ta sessiz alan-düşmesi olmaz).

## Canlıya açılış reçetesi (hosting kararı sonrası; ~15 dk)

```bash
# 1. Typesense sunucusu (Cloud ya da self-host docker) → URL + API key
export TYPESENSE_URL=https://...  TYPESENSE_API_KEY=...
# 2. (İlk kez / şema değişince) koleksiyon + tam yük:
python3 pipelines/search/upsert.py --recreate
# 3. Artımlı güncellemeler: aynı komut (action=upsert idempotent).
```

## Kabul
- [x] make test 158 · env-guard net mesajlı · emit/upsert offline-testli.
