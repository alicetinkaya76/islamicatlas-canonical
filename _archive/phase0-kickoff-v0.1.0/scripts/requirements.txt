# islamicatlas-canonical — Python dependencies
# Python 3.9+ required

# ─── Week 1 (audit) ───────────────────────────────────────
# week1_audit.py — standart kütüphane dışı bağımlılık yok

# ─── Week 2 (schema validation) ───────────────────────────
jsonschema>=4.20
pydantic>=2.5

# ─── Week 3-4 (entity resolution) ─────────────────────────
# Bu paketler ER pipeline başladığında eklenecek:
# rapidfuzz>=3.5              # Fuzzy string matching (Jaro-Winkler, etc.)
# python-Levenshtein>=0.23    # Fast edit distance
# pyarabic>=0.6.15            # Arabic text normalization
# unidecode>=1.3              # Transliteration fallback
# streamlit>=1.30             # Verification UI

# ─── Week 5 (ETL + DB) ────────────────────────────────────
# PostgreSQL + PostGIS etkileşimi için (Week 5 başında eklenir):
# sqlalchemy>=2.0
# alembic>=1.13
# psycopg[binary]>=3.1
# geoalchemy2>=0.14

# ─── Week 6 (API) ─────────────────────────────────────────
# fastapi>=0.109
# uvicorn[standard]>=0.27
# pyld>=2.0                   # JSON-LD

# ─── Week 7-8 (search + test) ─────────────────────────────
# typesense>=0.21
# pytest>=7.4
# httpx>=0.26                 # API integration tests

# ─── Dev tools (tüm faz) ──────────────────────────────────
ruff>=0.1.14
mypy>=1.8
